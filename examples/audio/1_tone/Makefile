# Tone Generator Example
# Generates a simple tone using the SPC700 DSP

OPENSNES := $(shell cd ../../.. && pwd)

# ROM Configuration
TARGET   := tone.sfc
ROM_NAME := OPENSNES TONE

# Source files
CSRC     := main.c

# Include shared build rules
include $(OPENSNES)/make/common.mk
