# Minimal Example
# Tests the new cc65816 compiler

OPENSNES := $(shell cd ../.. && pwd)

# ROM Configuration
TARGET   := minimal.sfc
ROM_NAME := OPENSNES MINIMAL

# Include shared build rules
include $(OPENSNES)/make/common.mk
