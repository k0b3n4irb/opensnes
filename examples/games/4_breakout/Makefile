OPENSNES := $(shell cd ../../.. && pwd)
TARGET   := breakout.sfc
ROM_NAME := BREAKOUT

CSRC     := main.c
ASMSRC   := data.asm

USE_LIB  := 1
LIB_MODULES := console sprite dma background input

include $(OPENSNES)/make/common.mk
