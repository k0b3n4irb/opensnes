# Text Module Test — Minimal text module validation

OPENSNES := $(shell cd ../../.. && pwd)

TARGET   := text_test.sfc
ROM_NAME := OPENSNES TEXT TEST

USE_LIB     := 1
LIB_MODULES := console dma text background sprite

CSRC   := main.c

include $(OPENSNES)/make/common.mk
