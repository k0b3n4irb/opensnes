# Types Unit Test

OPENSNES := $(shell cd ../../.. && pwd)

# ROM Configuration
TARGET   := test_types.sfc
ROM_NAME := OPENSNES TYPES TEST

# Source files (test_harness.c is #included directly)
CSRC     := test_types.c

# Include shared build rules
include $(OPENSNES)/make/common.mk
