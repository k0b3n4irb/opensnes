# Migrated from probes/audio.py (sfx): the SPC driver is alive -> ARAM populated
# (driver image + BRR samples uploaded). Footprint floor stands in for the
# probe's spc_stopped==false liveness check.
rom = "../../../examples/audio/snesmod_sfx/sfx.sfc"
steps = 5000000
[asserts.footprint]
aram = { nonzero_min = 2000 }
