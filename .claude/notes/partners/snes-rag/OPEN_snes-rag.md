# OpenSNES → snes-rag: open items (accumulating, not yet sent)

Opened 2026-09-26. One line per gap, with what reproduces it. Renamed to
its send date when it goes out (`.claude/rules/partners.md`).

| Date | Item | Reproduce | What we need |
|---|---|---|---|
| 2026-09-26 | **The six sources added on 2026-09-24 look listed but not indexed.** `snes_sources` shows 207 captured of 234, `ultrastarfox`, `argsfx-sasm-docs`, `peterlemon-gsu`, `sd2snes-changelog`, `cartouche-fiches`, `cartouche-fiches-jeux` dated 2026-09-24 — but the index still has **30848 chunks**, the count of the 2026-09-12 build with 201 sources, and the fingerprint is still `e2a738a553ec` although the build date (2026-09-25T18:28) and chunker (v7) changed. | `snes_sources()` → footer line. Then three targeted queries, `exclude_sources=["opensnes-docs","opensnes-notes-tech"]`, `k=6`: "Star Fox source code ARGSFX macro assembler GSU interrupt handler NMI WRAM stub"; "sd2snes firmware changelog: which version added SuperFX (GSU) support and SA-1 support"; "PeterLemon SNES GSU test ROM CHIP/GSU plot pixel" — **zero** chunks from any of the four GSU sources. The audit agent saw the same over 8 queries on 2026-09-26. | Index them, or tell us they failed to chunk and why. And a fingerprint that moves when the source set or the chunker moves: ours is the test "how to tell the corpus changed" (`.claude/notes/tech/cartouche_corpus.md`) and it no longer works. |
| 2026-09-26 | **Our item 2.5 of 2026-09-24 is answered — by `sd2snes-blog`, not by the changelog.** The FXPak Pro runs SA-1 and Super FX: v1.10.3 notes, April 2019, "[All] Fix swapped logic terms in SA-1 and SuperFX RAM write cycles … on Mk.II units; the bug was also present in the Pro firmware" (`c94f64959972104e`). We had inferred the opposite from an omission (`sfc-dev-wiki` chip list `4ec0785bcc6b469b`, which predates the cores) — our error, corrected in our tree the same day. | `snes_get("c94f64959972104e")` | Once `sd2snes-changelog` is indexed: the version that first shipped each core, so our hardware protocol can name a minimum firmware. A banner on `4ec0785bcc6b469b` saying the list is older than the SA-1 / Super FX cores would stop the next reader making our mistake. |
| 2026-09-26 | **Positive: fullsnes's GSU section now answers as arbiter** (our 2.1 of 2026-09-24 said it never surfaced). SCMR bit layout with RAN = bit 3, RON = bit 4 (`725e8061d576404e`), PLOT / RPIX / GETC opcodes (`b56b9ef201d2e8e1`). | "PeterLemon SNES GSU test ROM CHIP/GSU plot pixel" | Nothing — noted so you know the chunker v7 change reached it. |
| 2026-09-26 | **`luna-docs` is still the 2026-09-12 capture**; luna has tagged v1.25.0, v1.26.0 and v1.27.0 since (v1.27.0 on 2026-09-25: `gsu` state block, `bus_violations`, `--gsu-bus-trace`, per-job GSU profile, `--gsu-pc-set`, MCP `run_until_gsu_stop/go`). Our 3.3 of 2026-09-24 asked for a re-capture at every luna tag. | `snes_sources()` → `luna-docs — arbitre-domaine — 2026-09-12` | Re-capture at v1.27.0. |
| 2026-09-26 | **A fact we established that no captured source states in one place — CGWSEL.** Not a gap, a confirmation: anomie-regs (`f82b3b9d49563934`), fullsnes (`bfde55bff33073b1`) and snesdev-wiki (`c8d4aa2d4e954b71`) agree that bits 5-4 are the colour-math region and bits 7-6 the clip-to-black region, with no sub-screen twin. Our own tutorial had invented one (`colorMathSetMaskSub`); fixed. The fullsnes banner about 4-5 vs 6-7 was the right banner on the right topic this time. | "CGWSEL $2130 bits 4-5 color math enable region … bits 6-7 force main screen black" | Nothing. |
