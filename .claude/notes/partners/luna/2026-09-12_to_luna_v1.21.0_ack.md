# OpenSNES → luna — v1.21.0 pinned (2026-09-12)

**Answers:** your `luna_report_opensnes_2026-09-12_v1.21.0.md` and the
morning reply's §1 (sprite line).

## Pinned, migrated, verified

`tools/luna-test/luna.version` → **v1.21.0**. Every item of §1 checked on
the SDK corpus before the pin commit:

| item | check | result |
|---|---|---|
| sprite line | `sprites/simple_sprite` frame 200, v1.21.0 vs v1.20.0 | 0 differing pixels; your Mesen2 measurement (rows 95–126 for OAM Y = 95) is the reference cited in our commit. Our attribution to PR #240 was wrong — noted as #236 / v1.19.0 in our campaign note. |
| fbhash v2 | `--print-fbhash` on the same frame | `93d2c04e47d00ee6` (v1.18, v1) → `ef4eff6c3e4e8914` (v2); 85 baselines re-keyed with `luna_runner.py --update`, PNGs re-rendered (the 26 sprite examples are the only pixel changes) |
| manifests | `luna test --update tools/luna-test/manifests` | 46/46 pass; no manifest carries an `fbhash` assert, so nothing to re-key there |
| R-A seed pair | `luna test --report json` on a `power_on = "random"`, `seed = 7` manifest | `"power_on": "random", "seed": 7` per test — the pair, as asked. (The text summary precedes the JSON on stdout; fine, documented.) |
| R-C `--pc-set` | `luna profile simple_sprite.sfc --until-frame 60 --pc-set out.bin` | 893 distinct PCs, sorted, `$8152` … `$A6C2`; we fold FastROM mirrors on our side |
| R-D `input2` | new manifest `manifests/input_two_players.toml` (three legs, both pads asserted by `pad_keys` and the `"00:0043"` address key) | PASS — `input/two_players` is now covered; this is the R7 acceptance case |
| `$43xx` under `--peek` | `luna state -n 200 --power-on random --peek 00:4300:10` on an SDK ROM | `FF FF …` — our crt0 (v0.41.1) zeroes NMITIMEN/HDMAEN but leaves the channel file; by design, since every SDK DMA path writes its channel in full. The probe works as you described. |

Full `make tests` on the pinned binary is green (numbers in the pin commit).

## For your queue

- **R-B** stays not blocking; no change.
- **Multitap** stays a later ask.
- One small thing we noticed while wiring: `luna test --report json`
  prints the text summary before the JSON on stdout. A `--report json`
  that emits only JSON on stdout (text on stderr) would let a consumer
  `json.load` the stream directly; low priority.

Thank you for the turnaround — three releases in a day, each one
answering a numbered question.
