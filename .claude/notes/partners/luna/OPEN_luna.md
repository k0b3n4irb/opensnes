# OpenSNES → luna: open items (accumulating, not yet sent)

Opened 2026-09-26. One line per item. Every item is re-checked on the pinned
luna the day the report goes out (`.claude/rules/partners.md`); the pin is
v1.24.0 until the bump to v1.27.0 lands.

| Date | Item | Seen on | What we would run |
|---|---|---|---|
| 2026-09-26 | **R5 — SA-1 parity with the GSU profile.** `state.sa1` is `{pc, pb, p, running}`; `profile` has no SA-1 key (`budgets, end_frame, entries, frames, from_frame, instructions, rom, total_mclk`). Our SA-1 tutorial claims "10.74 MHz, 3×"; the corpus (higan speed table `7c52e450eef77f7d`) gives 10.74 MHz only for I-RAM code against a WRAM-bound S-CPU, ~5.4 MHz ROM against ROM — the case of our `sa1_starfield`. We cannot measure which one we are in. | v1.24.0 and v1.27.0, re-checked 2026-09-26: `luna state examples/chips/sa1_starfield/sa1_starfield.sfc --until-frame 120 --out -` → `sa1: {pc, pb, p, running}`; `luna profile … --out -` keys `budgets, end_frame, entries, frames, from_frame, instructions, rom, stack, total_mclk`. | `state.sa1.instructions_executed`, and in `profile` a `sa1` block with instructions, cycles, and cycles lost to bus conflicts with the S-CPU (the SA-1 analogue of `stall_cycles`). Same shape as `gsu.per_job` minus the jobs. |
| 2026-09-26 | **`--superfx-trace-from <frame>`** — you offered it; we saw it is already on your `develop` (`ffe04ac feat(cli): --superfx-trace-from`), not in a tag yet. Nothing to ask: we will use it from the next tag, for phase C of the Super FX runtime (IRQ on STOP). | luna `develop` log, 2026-09-26 | `luna run superfx_3d.sfc --superfx-trace out.csv --superfx-trace-from 300` |
| 2026-09-26 | **`port1` / `port2` keys in `luna test` manifests** (`pad` / `mouse` / `superscope` / `multitap` / `none`, like the CLI flags of v1.26.0). A manifest can plug a mouse (port 1) or a scope (port 2) through their scripts, but cannot unplug a port, so the `padIsConnected()` fix we are shipping can only be tested through `luna state --port1 none` in a Python fixture, not in a manifest. | v1.27.0: `crates/luna-cli/src/test_cmd.rs` manifest struct has `input`, `input2`, `mouse`, `superscope`, no port field | `port1 = "none"` at manifest or checkpoint level, then `[checkpoint.values] r_pad_conn = 0` |
| 2026-09-26 | **`symbol+N` keys in `[asserts.values]` / `[checkpoint.values]`.** Keys resolve a symbol or `BANK:OFFSET` (`resolve_key`, `test_cmd.rs`), so an array element (`results[8]`, `bg_scroll_x[2]`) can only be named by its raw address. Three of our manifests broke the day a 2-byte variable moved RAM by +2 (hwmath, ppumul, color_transparency) — nothing wrong in the ROMs. | v1.27.0 source, `crates/luna-cli/src/test_cmd.rs:1101` | `"results+16" = 0xFFFF`, `"bg_scroll_x+4" = { eq = 58, width = 2 }` |
