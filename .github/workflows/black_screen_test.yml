name: Black Screen Test

# ------------------------------------ #
# ROM validation using Mesen2 emulator #
# SHELVED: Mesen2 NativeAOT binary     #
# crashes in testrunner mode on CI.    #
# See memory/black_screen_ci.md        #
# ------------------------------------ #

# Only run manually until the headless issue is resolved
on:
  workflow_dispatch:

jobs:
  black-screen-test:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    continue-on-error: true

    steps:
      - name: Status
        run: |
          echo "Black screen test is shelved."
          echo "Mesen2 NativeAOT binary crashes with std::bad_cast in testrunner mode."
          echo "Next step: build Mesen2 from source as framework-dependent .NET app."
          echo "See: memory/black_screen_ci.md for full investigation notes."
